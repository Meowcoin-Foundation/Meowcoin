--- a/qtbase/src/corelib/global/qendian.h
+++ b/qtbase/src/corelib/global/qendian.h
@@ -41,6 +41,16 @@
 #include "qglobal.h"
 #include "qfloat16.h"
+// Make std::numeric_limits visible (GCC 11+), and avoid Windows min/max macros
+#include <limits>
+#ifdef _WIN32
+#  ifdef max
+#    undef max
+#  endif
+#  ifdef min
+#    undef min
+#  endif
+#endif
